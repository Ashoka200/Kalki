{
  "name": "kalki",
  "private": true,
  "description": "Server-side dependencies for the Netlify Functions only — the app in public/ stays zero-dependency.",
  "dependencies": {
    "web-push": "^3.6.7",
    "@netlify/blobs": "^8.1.0"
  }
}
